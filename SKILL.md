---
name: getjobs-skill
description: 求职全流程助手，支持 JD 抓取、简历匹配、投递追踪和简历联动。用于求职者管理招聘流程、搜索目标岗位、分析匹配度、追踪投递状态；当用户要求搜索职位、分析 JD 匹配度、管理投递记录或生成定制简历时使用。
---

# GetJobs - 求职全流程助手

## 目标

帮助求职者高效管理求职全流程：搜索目标职位、自动抓取 JD、分析与简历的匹配度、追踪投递状态，并在适当时机调用 resume-jd-optimizer-cn 生成定制简历和求职信。

## 先遵守的硬规则

1. **JD 真实性优先**：只使用来自可信来源的 JD（用户提供、官网抓取、招聘平台 API），不编造或臆断职位信息。
2. **隐私保护**：不保存用户敏感信息（身份证、银行卡、真实地址），投递记录本地存储，不上传云端。
3. **平台规则尊重**：抓取行为遵守 robots.txt 和平台使用条款，不批量爬取、不绕过付费墙。
4. **联动前提**：调用 resume-jd-optimizer-cn 前必须获得用户明确授权。
5. **状态同步**：投递状态变更必须更新追踪文件，并告知用户操作结果。
6. **信息透明**：JD 来源必须可追溯，抓取失败时明确告知用户。
7. **用户控制权**：所有关键操作（确认 JD、抓取来源、投递记录修改）需用户确认。

## 适用与不适用

适用：搜索目标公司/岗位、抓取 JD 内容、分析与简历匹配度、管理投递记录、生成定制简历和求职信、追踪面试进度。

不适用：批量爬取招聘平台、绕过平台付费限制、伪造投递记录、自动发送简历（需用户手动操作）、获取用户未授权的隐私信息。

## 输入要求

优先收集：目标公司/岗位、薪资范围偏好、地点偏好、工作类型（全职/实习/远程）、当前简历、追踪文件路径。

若用户未提供简历，匹配分析只能基于 JD 本身；若用户未提供追踪文件路径，按默认路径创建。

## 阶段响应契约

| 场景 | 可执行 | 必须输出 | 禁止输出 |
|---|---|---|---|
| A：职位搜索 | JD 搜索与抓取 | JD 列表、来源、可信度评估 | 未验证的职位信息 |
| B：JD 分析 | JD 解析、匹配度计算 | JD 要求表、匹配分析、差距说明 | 用户匹配结论 |
| C：追踪管理 | 读取/创建/更新记录 | 投递列表、状态摘要、待跟进项 | 虚假状态 |
| D：完整求职流 | 全流程执行 | 匹配报告、追踪记录、简历/求职信 | 未确认信息 |
| E：简历联动 | 调用 resume-jd-optimizer-cn | 定制简历、求职信、更新追踪 | 未经授权的操作 |

若用户只说"找工作"而未提供具体方向，按路由 A 处理：先询问目标公司/岗位/地点。

## 功能模块

### 1. JD 抓取（Job Crawler）

**输入**：公司名、岗位名、平台名称（可选）

**执行**：
1. 检查是否配置了招聘平台 API
2. 若无 API，使用 browser 插件自动化抓取：
   - 优先抓取官网招聘页
   - 次选招聘平台（需尊重平台规则）
3. 解析 JD 内容，提取关键字段

**输出字段**：
| 字段 | 说明 | 必填 |
|---|---|---|
| company | 公司名 | 是 |
| position | 岗位名 | 是 |
| salary_range | 薪资范围 | 否 |
| location | 工作地点 | 否 |
| job_type | 全职/实习/远程 | 否 |
| requirements | 任职要求 | 是 |
| responsibilities | 岗位职责 | 是 |
| preferred | 加分项 | 否 |
| source_url | JD 来源链接 | 是 |
| source_platform | 来源平台 | 是 |
| crawled_at | 抓取时间 | 是 |

### 2. JD 解析（JD Parser）

**复用** resume-jd-optimizer-cn 的 `prompts/jd_parser.md` 解析逻辑。

**扩展字段**：
- 薪资结构（base/bonus/equity）
- 招聘紧迫度（急招/正常/储备）
- 团队规模
- 汇报对象

### 3. 简历匹配（Resume Matcher）

**复用** resume-jd-optimizer-cn 的 JD-简历匹配逻辑。

**新增分析**：
- 多 JD 批量对比
- 竞争力排名
- 薪资谈判建议

### 4. 投递追踪（Application Tracker）

**本地存储**：`~/.getjobs/applications.json`

**追踪字段**：
| 字段 | 说明 |
|---|---|
| id | 唯一标识符 |
| platform | 投递平台 |
| company | 公司名 |
| position | 岗位名 |
| jd_summary | JD 核心摘要 |
| match_score | 匹配分（0-100） |
| applied_date | 投递日期 |
| status | 投递状态 |
| follow_up_date | 跟进日期 |
| resume_version | 使用的简历版本 |
| cover_letter | 是否生成求职信 |
| notes | 用户备注 |
| last_updated | 最后更新时间 |

**状态流转**：
```
pending → viewed → interview → offer
                  ↘ rejected
         ↘ withdrawn
```

### 5. 简历联动（Resume Integration）

**调用流程**：
1. 用户选择目标 JD
2. 提取 JD 核心要求
3. 调用 resume-jd-optimizer-cn 生成定制简历
4. 可选生成求职信
5. 更新追踪记录

## 输出格式

### JD 搜索结果

```markdown
## 搜索结果

| # | 公司 | 岗位 | 薪资 | 地点 | 来源 | 可信度 |
|---|---|---|---|---|---|---|
| 1 | xxx | xxx | xxx | xxx | [链接] | ✅ 高 |

### JD 详情
[完整 JD 内容]
```

### 匹配分析报告

```markdown
## 匹配分析

| 匹配维度 | 得分 | 说明 |
|---|---|---|
| 硬性条件 | 85/100 | 符合年限要求 |
| 技能匹配 | 72/100 | SQL 弱 |
| 经验相关度 | 80/100 | B 端经验匹配 |

### 关键差距
[已覆盖 / 弱覆盖 / 未覆盖 分析]
```

### 投递追踪摘要

```markdown
## 投递追踪

**本周投递**：3 个
**待跟进**：2 个
**面试中**：1 个

### 详情
[追踪列表]
```

## 数据存储

### 默认存储路径
- 配置文件：`~/.getjobs/config.json`
- 投递记录：`~/.getjobs/applications.json`
- 缓存 JD：`~/.getjobs/cache/jds/`
- 生成的简历：`~/.getjobs/output/resumes/`

### 隐私原则
- 所有数据本地存储，不上传云端
- 敏感信息需用户明确授权才保存
- 每次写入前备份原文件

## 与 resume-jd-optimizer-cn 联动

当用户请求生成定制简历时：

1. **确认授权**：询问是否允许调用简历 Skill
2. **提取 JD**：获取目标岗位的 JD 内容
3. **调用 Skill**：使用 `$resume-jd-optimizer-cn` 执行简历优化
4. **保存结果**：将生成的简历保存到 `~/.getjobs/output/resumes/`
5. **更新追踪**：在投递记录中关联简历版本

### 联动触发条件
- 用户明确请求"生成简历"
- JD 已抓取且用户确认
- 用户简历已提供或可获取

### 联动禁止
- 未经用户授权自动调用
- 在 JD 未确认时生成简历
- 将生成的简历自动投递

## 支持的平台

### 国内平台
| 平台 | 优先级 | 抓取方式 |
|---|---|---|
| 官网招聘页 | 最高 | API / 浏览器 |
| Boss 直聘 | 高 | 用户提供 JD |
| 拉勾 | 高 | 用户提供 JD |
| 猎聘 | 中 | 用户提供 JD |
| 智联招聘 | 中 | 用户提供 JD |
| 前程无忧 | 中 | 用户提供 JD |

### 海外平台
| 平台 | 优先级 | 抓取方式 |
|---|---|---|
| LinkedIn | 高 | 用户提供 JD |
| Indeed | 高 | API / 浏览器 |
| Glassdoor | 中 | 浏览器 |
| RemoteOK | 中 | API |
| AngelList | 中 | 浏览器 |

### 平台规则
- 优先抓取官网 JD（信息最准确）
- 招聘平台 JD 需标注来源
- 尊重平台反爬规则，不强制抓取

## 真实性与质量验收

在交付前逐条确认：

- JD 来源可追溯，链接有效
- 匹配分析有 JD 原文依据
- 追踪记录变更已保存
- 简历联动获得用户授权
- 敏感信息已脱敏处理

## 相关资源

- 简历优化：`resume-jd-optimizer-cn`
- JD 解析规则：`prompts/jd_parser.md`
- 匹配评分规则：`rubrics/match_score.md`
- 平台使用指南：`docs/platform_guide.md`
- 存储格式说明：`docs/storage_format.md`
- 隐私保护政策：`docs/privacy.md`
